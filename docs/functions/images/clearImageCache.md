# clearImageCache

Clears the internal image cache that stores loaded local images and downloaded remote image resources. This is useful for clearing memory in persistent environments or after rendering templates that use massive collections of distinct images.

## Syntax

```bash
$clearImageCache[]
```

## Parameters

This function does not take any parameters.

## Example

```bash
$clearImageCache[]
```
